# Moltbot SSH server configuration
# This file is included by /etc/ssh/sshd_config

# Allow password authentication (for initial setup)
PasswordAuthentication yes

# Allow the moltbot user
AllowUsers moltbot

# Disable root login
PermitRootLogin no

# Use PAM for authentication
UsePAM yes
